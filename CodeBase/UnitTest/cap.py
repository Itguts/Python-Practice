def cap_text(text):
    '''
    Input a string
    And capitalize it
    '''
    # first run for this capitalize method, it will fail for more than one word string
    #return text.capitalize()
    return text.title()
